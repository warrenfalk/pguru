{
  "extends": "./tsconfig.commit.json",
  "compilerOptions": {
    /* Here we relax some of the rules that make development irritating
       These rules are still enforced before a commit */
    "noUnusedLocals": false,
    "noUnusedParameters": false,
  },
}
